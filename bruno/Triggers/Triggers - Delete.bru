meta {
  name: Triggers - Delete
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/v1/workspaces/:workspace/triggers/:trigger
  body: none
  auth: inherit
}

params:path {
  workspace: {{workspaceId}}
  trigger: {{triggerId}}
}

docs {
  Deletes a trigger. Requires the `trigger.manage` permission.

  Responds `204 No Content` with no envelope.
}
