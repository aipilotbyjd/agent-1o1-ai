meta {
  name: Onboarding - Submit Discovery
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/onboarding/discovery
  body: json
  auth: inherit
}

body:json {
  {
    "discovery_source": "google_search"
  }
}

docs {
  Records how the user found the product and advances onboarding to the
  final `discovery_survey` step.
}
