meta {
  name: Notification Channels - Test
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/v1/workspaces/:workspace/notification-channels/:notificationChannel/test
  body: none
  auth: inherit
}

params:path {
  workspace: {{workspaceId}}
  notificationChannel: {{notificationChannelId}}
}
